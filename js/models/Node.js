/**
 * Represents a node in a linked list
 */
class Node {
  /**
   * Creates a new Node
   * @param {*} data - The data to store in this node
   */
  constructor(data) {
    this.data = data;
    this.next = null;
  }
}
